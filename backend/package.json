{
  "name": "uk-financial-comparison-backend",
  "version": "1.0.0",
  "description": "UK Financial Products Comparison Platform - Lambda backend",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@google/generative-ai": "^0.21.0",
    "csv-parse": "^5.5.6"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "aws-sdk-client-mock": "^4.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageThreshold": {
      "global": {
        "branches": 70,
        "functions": 80,
        "lines": 80
      }
    }
  }
}
